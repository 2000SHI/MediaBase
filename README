# Start the project

1. Set required environment variables in `.env` according to `.env.example`
2. Modify `nginx/nginx.conf` to change `upstream backends` according to the number of servers
3. Run `docker compose up --build [--scale backend=<number of servers>]`